// Iotift Standard Library: gpio
// Digital GPIO functions for ESP32/Arduino.

extern fn digitalRead(int pin) -> int;
extern fn digitalWrite(int pin, int value);
extern fn pinMode(int pin, int mode);
extern fn toggle(int pin);
