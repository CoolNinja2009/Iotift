# Iotift Standard Library
# These modules are .iot files that declare extern functions and constants
# available to all Iotift programs via the prelude (time, math, gpio)
# or explicit import (serial, i2c, spi, pwm).
